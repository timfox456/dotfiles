# Global rules

- Be concise: no preamble, no closing summaries unless asked.
- Minimal diffs; match the file's existing style and conventions.
- Never commit or push unless explicitly asked.
- For questions, prefer read-only commands over editing files.
